{
  "name": "tests",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright tests for oepsbanaan.nl",
  "devDependencies": {
    "@playwright/test": "1.48.2"
  },
  "scripts": {
    "test": "playwright test --config playwright.config.js",
    "test:pr": "PROJECT_SET=pr playwright test --config playwright.config.js",
    "test:project": "playwright test --config playwright.config.js --project",
    "lint": "node -e \"process.exit(0)\""
  }
}
