# Frontend tooling: eslint, stylelint, terser, csso
ARG NODE_TAG=25-alpine
FROM node:${NODE_TAG}

WORKDIR /work

RUN npm install -g \
  eslint \
  stylelint stylelint-config-standard \
  terser \
  csso-cli
